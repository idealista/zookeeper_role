---

- hosts: all
  pre_tasks:
    - name: installing net-tools
      apt:
        pkg: "{{ item }}"
        state: present
      with_items:
        - net-tools

- hosts: zookeeper
  roles:
    - java
    - zookeeper-role
